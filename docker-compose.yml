services:
  hikvision-webhook:
    build: .
    container_name: hikvision-webhook
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - PORT=${PORT:-8080}
      - ALLOW_IPS=${ALLOW_IPS}
      - LOG_DIR=/data/logs
    volumes:
      - ./data:/data
    restart: unless-stopped
