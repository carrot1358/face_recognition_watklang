# พอร์ตที่เว็บฮุคฟัง (ต้องตรงกับที่ตั้งในเครื่อง Hikvision)
PORT=8080

# ระบุรายการ IP ที่อนุญาตให้ส่งเหตุการณ์เข้ามาได้ (คั่นด้วย comma)
# เช่น ALLOW_IPS=192.168.1.201,192.168.1.202
# ถ้าเว้นว่าง = อนุญาตทุกแหล่ง (ไม่แนะนำใน production)
ALLOW_IPS=

MINIO_ENDPOINT=minio-api.sylorqynvex.cc
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=password123
MINIO_BUCKET=hikvision-images

# PostgreSQL Database URL
# Format: postgresql://username:password@localhost:5432/database_name
# แก้ไขให้ตรงกับ PostgreSQL server ของคุณ
DATABASE_URL="postgresql://user:password@domain:port/hikvision_db"
